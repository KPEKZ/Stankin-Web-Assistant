interface Idepartment {
  id?: number;
  name ?: string;
  instituteId ?: number;
  urlProfile ?: string;
  shortName ?: string;
}

export interface department extends Idepartment {};
export interface department2 extends Idepartment {};
